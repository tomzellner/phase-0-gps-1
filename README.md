made new repo on github
clone the repo from github
	$git clone [url]
created the awesome_page.md
	$touch awesome_page.md
add, commit, push to master
created new branch
	$git checkout -b [branch name]
